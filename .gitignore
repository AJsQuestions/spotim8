# ============================================
# Spotim8 - Git Ignore Configuration
# ============================================

# ----- Python -----
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
spotim8.egg-info/

# ----- Virtual Environments -----
.env
.venv
env/
venv/
ENV/

# ----- IDE / Editor -----
.idea/
.vscode/
*.swp
*.swo
*.sublime-project
*.sublime-workspace

# ----- Build Tools -----
.vite/
dist/
build/
*.egg-info/

# ----- macOS -----
.DS_Store
*.DS_Store
Thumbs.db

# ----- Jupyter Notebook -----
.ipynb_checkpoints/

# ----- Spotify Credentials & Cache -----
.cache
.cache-*
*.cache
notebooks/.cache

# ----- Data Files -----
# Ignore ALL data files and folders (user-specific synced library data)
# These are generated locally and contain personal information - NEVER commit
# Ignore entire data directory and all contents
data/
# But allow a README if needed for documentation
!data/README.md
# Explicitly ignore all parquet files (user data)
*.parquet
# Explicitly ignore all JSON files in data/ (personal Spotify exports)
data/**/*.json
# Explicitly ignore all CSV files in data/ (analysis outputs)
data/**/*.csv

# Ignore local cache and metadata (regenerated on sync)
data/.api_cache/
data/catalog_meta.json
data/meta.json

# User-specific analysis outputs
data/playlist_consolidation_recommendations.csv

# Checkpoint files (for resuming interrupted syncs)
data/.liked_sync_offset
data/.artist_sync_done

# ----- Logs -----
*.log
logs/
logs/*.lock

# ----- Environment Variables -----
.env
.env.local
.env.*.local
.env.bak
.env.backup
*.env.bak

# ----- iOS App Projects -----
# Ignore Xcode projects and build artifacts
apps/ios/Spotim8app/
ios_app/
*.xcodeproj/
*.xcworkspace/
*.xcuserdata/
DerivedData/
build/
*.app
*.ipa
*.dSYM.zip
*.dSYM
